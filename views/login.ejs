<%- include('./partials/header') %>
    <div class="flex items-center justify-center min-h-screen bg-gray-50">
        <div class="w-full max-w-sm p-6 bg-white border border-gray-200 rounded-md shadow-sm">
            <h3 class="text-2xl text-center font-medium text-gray-800 mb-2">Welcome to <span class="text-blue-500 font-semibold">MapMeet</span></h3>
            <h4 class="text-lg text-center text-gray-600 mb-4">Login your account</h4>
            <form action="/users/login" method="post">
                <div class="mb-3">
                    <input
                        class="block w-full px-3 py-2 text-sm border border-gray-300 rounded-md bg-gray-100 focus:ring-1 focus:ring-blue-500 focus:outline-none"
                        type="email" placeholder="Email" name="email" required>
                </div>
                <div class="mb-3">
                    <input
                        class="block w-full px-3 py-2 text-sm border border-gray-300 rounded-md bg-gray-100 focus:ring-1 focus:ring-blue-500 focus:outline-none"
                        type="password" placeholder="Password" name="password" required>
                </div>
                <button
                    class="w-full py-2 text-sm font-medium text-white bg-blue-500 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400">
                    Login
                </button>
            </form>
            <p class="mt-4 text-center text-sm text-gray-500">
                Don’t have an account? <a href="/register" class="text-blue-500 hover:underline">Register</a>
            </p>
                    <div class="mt-2 text-center">
                        <span class="text-slate-500">
                            or you can sign in with &nbsp;
                        </span>
                        <a href="/auth/google"><i class="fa-brands fa-google"></i></a>
                    </div>
        </div>
    </div>
<%- include('./partials/footer') %>